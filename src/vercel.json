{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/timbua-mvp/browser",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}